{
  "name": "exec0",
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/aws-lambda": "8.10.159",
    "@types/bun": "latest",
    "sst": "^3.17.25",
    "turbo": "^2.6.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "packageManager": "bun@1.3.4",
  "private": true,
  "scripts": {
    "dev": "bun run turbo dev",
    "format": "biome format --write",
    "lint": "biome check --fix",
    "auth:generate": "@better-auth/cli@latest generate --config ./packages/auth/src/lib/auth.ts --output ./packages/database/prisma/schema.prisma"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
